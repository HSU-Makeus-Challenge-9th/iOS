import SwiftUI

struct MobileOrderView: View {
    var body: some View {
        VStack {
            Text("모바일 오더 페이지입니당")
                .font(.title)
                .padding()
            
            Spacer()
        }
        .navigationTitle(Text("모바일 오더"))
    }
}
